
Remove the /tsugi/lms code once we verify it is not used.

Create badge table to "mint" badges so they are self contained
and independent of other data.

Move pre_config into setup.php

Clean up the koseu folder.

Add feature to store browser mark in the outer iframe

Add feature to attempt to retrieve browser mark via postmessage when ip address changes

Remove auto login.

