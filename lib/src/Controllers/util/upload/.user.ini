; Only this executed script is the Settings cartridge importer.
; Other Tsugi pages keep the site-wide PHP post_max_size (typically 8M).
; post_max_size is above upload_max_filesize so multipart framing of a 128M file fits.
upload_max_filesize = 128M
post_max_size = 136M
